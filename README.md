# json-mini-parser
A minimalistic JSON parser in C that requires no extra memory

To build do the usual:
```
mkdir build
cd build
cmake ..
make -j
./json-mini-parser
```

In order to run tests just to `ctest`.

In order to see examples, look at `main.c`

